#include <string>
#include <classes.h>
#include <vector>

std::string double_to_string(double num, int precision); 

bool is_subset(std::vector<Chemical> superset, std::vector<Chemical> subset);